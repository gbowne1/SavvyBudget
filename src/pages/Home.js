import { Box, Typography } from '@mui/material';

function Home() {
  return (
    <Box>
      <Typography variant="h1">SavvyBudget</Typography>
      <Typography variant="h2">Overview</Typography>
      {/* Components for budget, remaining, and expense total */}
    </Box>
  );
}

export default Home;